/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com'], // Add the domain of Unsplash images here
  },
};

module.exports = nextConfig;
